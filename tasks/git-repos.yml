---
- name: Ensure git staging area exists.
  file:
    path: "{{ repo_location }}"
    state: directory

- name: Clone listed Git repos.
  git:
    repo: "{{ item.name | default(item) }}"
    dest: "{{ repo_location }}"
  loop: "{{ git_repos }}"
